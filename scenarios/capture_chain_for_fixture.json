{
  "name": "capture_chain_for_fixture",
  "description": "Capture 20 headers from genesis into a fixture file. Run with: cargo run -- --scenario scenarios/capture_chain_for_fixture.json --capture-fixture fixtures/devnet_genesis.jsonl",
  "target_address": "localhost:3001",
  "network_magic": 42,
  "trace_output_path": "trace_client.jsonl",
  "expected_outcome": "success",
  "steps": [
    { "kind": "connect" },
    { "kind": "handshake" },
    {
      "kind": "chain_sync",
      "count": 20
    },
    { "kind": "disconnect" }
  ]
}
