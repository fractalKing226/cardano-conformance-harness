{
  "name": "parallel_with_error",
  "description": "One branch sleeps for 10 seconds; the other immediately errors by referencing a connection name that was never created. The framework should abort the sleeping branch when the other fails, completing the scenario in well under 10 seconds.",
  "target_address": "localhost:3001",
  "network_magic": 42,
  "trace_output_path": "trace_parallel_with_error.jsonl",
  "expected_outcome": "failure",
  "steps": [
    {
      "kind": "parallel",
      "branches": [
        [
          { "kind": "connect", "as": "slow_branch" },
          { "kind": "handshake", "on": "slow_branch" },
          { "kind": "sleep", "duration_secs": 10 }
        ],
        [{ "kind": "handshake", "on": "nonexistent_connection" }]
      ]
    }
  ]
}
