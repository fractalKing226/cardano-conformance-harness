{
  "name": "scripted_honest_serve",
  "description": "Serve a fixture chain using the auto-generated honest script (fixture_path form). Confirms that slice-2 behaviour is preserved after the script-execution refactor.",
  "target_address": "localhost:3001",
  "network_magic": 42,
  "trace_output_path": "trace_server.jsonl",
  "expected_outcome": "success",
  "steps": [
    { "kind": "listen", "bind_address": "0.0.0.0:3001" },
    { "kind": "accept_handshake" },
    {
      "kind": "serve_chain_sync",
      "fixture_path": "fixtures/devnet_genesis.jsonl",
      "await_at_tip_secs": 30
    },
    { "kind": "close_listener" }
  ]
}
